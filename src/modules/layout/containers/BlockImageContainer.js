import { React } from '../dependencies'

import BlockImage from '../components/BlockImage'

const BlockImageContainer = ({ opt }) => {
	return <BlockImage opt={opt} />
}

export default BlockImageContainer
